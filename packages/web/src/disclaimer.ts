// ============================================================
// CiteSight Web — accuracy disclaimer
// Re-exported from @michaelborck/cite-sight-core so the wording has a single
// source of truth across CLI, desktop, and web (no hand-mirrored text to keep
// in sync). The core /disclaimer subpath is browser-safe — a pure string
// constant with no Node dependencies — so it imports cleanly in the browser.
// ============================================================

export {
  DISCLAIMER,
  ATTRIBUTION,
  PACING_NOTE,
  HOSTED_LIMITS_NOTICE,
  HOSTED_LIMITS_SHORT,
} from '@michaelborck/cite-sight-core/disclaimer';
